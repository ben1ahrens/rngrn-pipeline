__pycache__/
*.pyc
*.egg-info/
.venv/
build/
dist/
# generated data & runs (regenerate from configs)
data/cache/
data/three_gene_dataset/*.h5
experiments/
data/datasets/*/payload.h5
data/staging/

# machine-local Claude Code overrides. The rest of .claude/ is TRACKED and shared: the
# agents, skills, rules and the trainer guard are project conventions, not personal setup.
.claude/settings.local.json
